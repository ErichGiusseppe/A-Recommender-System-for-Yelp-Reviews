venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
notebooks/
*.ipynb
.env
.env.*

# Raw Yelp dataset — only for generating parquets, not at runtime
data/yelp_dataset/

# Photos actual JPGs — served from GCS bucket gs://lantern-photos-rs26
data/real/photos/photos/
# Keep photos.json (business_id → photo_id mapping, 24MB)
# Keep data/real/photos/photos.json in the image

# Archives
data/*.zip

# Local SQLite — recreated at startup by init_db()
data/lantern.db

# Dev/doc scripts
generate_parquets.py
generate_reviews.py
generate_hours.py
generate_docs.py
generate_quick_ref.py
find_test_users.py
REFACTOR_PLAN.md
